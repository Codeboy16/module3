// Document Schema (Document.js)

// Fields: title, description, file, status (Pending, Rejected, Returned, Approved), currentStep (Operator, Verifier, Approver), history (array of action history), user (the user who submitted the document), etc.